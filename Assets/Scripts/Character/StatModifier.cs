public class StatModifier
{
    public string reason;
    public float delta;

    public StatModifier(string reason, float delta)
    {
        this.reason = reason;
        this.delta = delta;
    }
}